@page
@model RazorPagesMovie.Pages.ConnectProductMaterial.DeleteModel

@{
    ViewData["Title"] = "Удалить связь";
}

<h2>Удалить связь товара и материала</h2>

<div class="alert alert-warning">
    <strong>Вы уверены, что хотите удалить эту связь?</strong>
</div>

<div class="card">
    <div class="card-body">
        <dl class="row mb-0">
            <dt class="col-sm-3 fw-bold">Товар</dt>
            <dd class="col-sm-9">@Model.ConnectProductMaterial.IdProductNavigation?.Name</dd>

            <dt class="col-sm-3 fw-bold">Материал</dt>
            <dd class="col-sm-9">@Model.ConnectProductMaterial.IdMaterialNavigation?.MaterialName</dd>

            <dt class="col-sm-3 fw-bold">Количество</dt>
            <dd class="col-sm-9">@Model.ConnectProductMaterial.Quantity.ToString("F2")</dd>

            <dt class="col-sm-3 fw-bold">Ед. измерения</dt>
            <dd class="col-sm-9">@Model.ConnectProductMaterial.Unit</dd>
        </dl>
    </div>
</div>

<form method="post" class="mt-3">
    <input type="hidden" asp-for="ConnectProductMaterial.Id" />
    <button type="submit" class="btn btn-danger">Удалить</button>
    <a asp-page="./Index" class="btn btn-secondary ms-2">Отмена</a>
</form>